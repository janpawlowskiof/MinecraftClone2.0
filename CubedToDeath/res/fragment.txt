#version 450 core
out vec4 FragColor;

in vec2 TexCoords;

uniform sampler2D texture_terrain;

void main()
{
	vec4 color = texture(texture_terrain, TexCoords);
	if(color.a < 0.5) discard;
    FragColor = color;
}
